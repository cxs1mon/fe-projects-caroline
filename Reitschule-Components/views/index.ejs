<!DOCTYPE html>
<html lang="de">

<%- include('partials/head', { title: 'Willkommen' }) %>

    <body class="page container-fluid">

        <!--Header-->
        <rts-header></rts-header>

        <!--Main-->
        <main class="main row g-0">

            <!--Nav-->
            <div class="col col-lg-2">
                <rts-nav></rts-nav>
            </div>
            <div class="col-sm-12 col-lg-10">
                <section class="text-block">
                    <h2 class="text-block__title">Herzlich Willkommen</h2>
                    <figure class="image__figure">
                        <img class="image__img" src="./img/schnee-pferde.jpeg" alt="image of a horse stable">
                    </figure>

                    <rts-text-block 
                        title="<%= meinZiel.title %>"
                        text="<%= meinZiel.text %>">
                    </rts-text-block>
            
                    <rts-text-block 
                        title="<%= meineLeidenschaft.title %>"
                        text="<%= meineLeidenschaft.text %>">
                    </rts-text-block>
                </section>
            </div>
        </main>

        <!--Footer-->
        <rts-footer></rts-footer>

    </body>

</html>