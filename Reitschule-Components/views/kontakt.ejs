<!DOCTYPE html>
<html lang="de">

<%- include('partials/head', { title: 'Kontakt' }) %>

<body class="page container-fluid">

    <!--Header-->
    <rts-header></rts-header>

    <!--Main-->
    <main class="main row g-0">

        <!--Nav-->
        <div class="col col-lg-2">
            <rts-nav></rts-nav>
        </div>
        <div class="col-sm-12 col-lg-10">
            <section class="text-block">
                <h2 class="text-block__title">Kontakt</h2>
                <rts-text-block
                    title="<%= adressen.title %>"
                    text="<%= adressen.text %>">
                </rts-text-block>
                
                <rts-text-block
                    title="<%= lage.title %>"
                    text="<%= lage.text %>">
                </rts-text-block>

                <rts-text-block
                    title="<%= kontaktformular.title %>"
                    text="<%= kontaktformular.text %>">
                </rts-text-block>
                
            </section>
            
        </div>
    </main>

    <!--Footer-->
    <rts-footer></rts-footer>

</body>

</html>